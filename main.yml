---
- name: Base machine configuration
  hosts:
    - servers
  become: true
  become_user: root
  roles:
    - name: network_dns
    - name: network_dhcpcd
    - name: xbps
    - name: chrony
    - name: crond
    - name: soju
      when: soju_enabled | default(false)
